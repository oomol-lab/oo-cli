interface:
  display_name: oo Create Skill
  short_description: Create local OOMOL workflow skills
  default_prompt: "Use $oo-create-skill when the user asks to author, scaffold, generate, or update a local AI agent skill for an OOMOL/oo package, connector action, block, or workflow, including requests that need capability discovery before authoring."

policy:
  allow_implicit_invocation: true
